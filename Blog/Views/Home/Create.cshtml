@model CreatePostViewModel

@{
    Layout = "_Layout";
}

<h2 style="margin-top: 20px;">Create new post</h2>
<hr />
<div class="row">
    <div class="col-md-8">
        <form asp-action="Create" onkeypress="return event.keyCode != 13;">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Post.Title" class="control-label"></label>
                <input asp-for="Post.Title" class="form-control" />
                <span asp-validation-for="Post.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Post.Content" class="control-label"></label>
                <textarea asp-for="Post.Content" id="postContent" class="form-control" style="min-height: 300px;"></textarea>
                <span asp-validation-for="Post.Content" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tags" class="control-label"></label>
                <div id="tags">
                    @Html.HiddenFor(m => m.Tags, new { id = "modelTags" })
                    <input type="text" id="add_tag" placeholder="Add a tag......">
                    <div class="tags_dropdown"></div>
                </div>
            </div>
            <input id="image" type="hidden" asp-for="Post.Image"/>

            <div class="form-group">
                @Html.ActionLink("Cancel", "Index","Home", null, new { @class = "btn btn-danger" })
                <input type="submit" onclick="addTags()" value="Create" class="btn btn-primary"/>
            </div>


        </form>
    </div>

    <div class="col-md-4">
        <img id="postImg" onerror="changePic()"/>
        <button id="btnChangePic" class="btn btn-primary" style="margin: 10px 10px 0 0;">Change picture</button>
    </div>
</div>
@section Scripts
{
    <script src="~/js/Home/Create.js"></script>
    <script>      
        new inputTags({
            asd: true,
            id: 'add_tag',
            tags: ['day review'],
            maxTags: 20,
            allowDuplicateTags: false,
            onTagRemove: function (tag) {
                addTags();
            }
        });
    </script>
}